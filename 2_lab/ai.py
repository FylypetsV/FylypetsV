# Розділ 1: Виведення тексту
print("Привіт, світ!")

# Розділ 2: Змінні та операції
a = 5
b = 3
с = a + b
print(f"Сума {a} і {b} дорівнює {c}")

# Розділ 3: Умовний оператор
if c > 8:
    print("Сума більше 8")
else:
    print("Сума менше або дорівнює 8")

# Розділ 4: Цикли
for i in range(3):
    print(f"Це ітерація номер {i + 1}")

# Розділ 5: Функції
def привітання(ім'я):
    return f"Привіт, {ім'я}!"

повідомлення = привітання("Олександр")
print(повідомлення)

# Розділ 6: Списки та цикли зі списками
список_чисел = [1, 2, 3, 4, 5]
сума_списку = sum(список_чисел)
print(f"Сума чисел у списку: {сума_списку}")

# Розділ 7: Робота з рядками
рядок = "Це приклад рядка"
довжина_рядка = len(рядок)
print(f"Довжина рядка: {довжина_рядка}")

# Розділ 8: Обробка винятків
спроба:
    число = int("не число")
окрім ValueError as помилка:
    print(f"Виникла помилка: {помилка}")

# Розділ 9: Класи та об'єкти
клас Приклад:
    def __init__(self, значення):
        self.значення = значення

    def виведення_значення(self):
        print(f"Значення: {self.значення}")

приклад_об'єкту = Приклад("Деяке значення")
приклад_об'єкту.виведення_значення()
